
# Question
 
 1️⃣ What is the difference between var, let, and const?

Answer: var, let and const use for declare a variable. var can declare same value again but let and const can't. var and let can change the value but const can't change the value.

2️⃣ What is the spread operator (...)?

Answer: used for Array/Objects items separates.

3️⃣ What is the difference between map(), filter(), and forEach()?

Answer: map() can transform array's element, filter() can find the conditionally array element, forEach() can't return the value.

4️⃣ What is an arrow function?

Answer: arrow function is a shorter version from ES6 to written the traditional function.

5️⃣ What are template literals?

Answer: template literals is a flexible syntax to written string. it's written backticks(``).

